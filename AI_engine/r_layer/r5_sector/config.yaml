model_id: R5
model_name: Sector Models (per-sector LightGBM)
model_type: regression_per_sector
default_horizon: 5
min_sector_stocks: 3  # below this, use global model
scale_factor: 200
hyperparams:
  n_estimators: 150
  max_depth: 5
  learning_rate: 0.05
  verbose: -1
